# 覆盖矩阵（自动生成）

> 真值来源：`覆盖矩阵.yaml`。本页只展示需求和证据边界，不把未执行的真实资源或部署测试标成通过。

共 15 项需求：已覆盖 0，部分覆盖 15，未覆盖 0，阻断 0。

| 编号 | 业务域 | 用户目标 | 开始状态 | 成功结果 | 技术事实 | 失败/恢复 | 资源 | 层级 | 场景 | 证据 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| KM-INIT-001 | 初始化与存储 | 建立第一个 Local 桶、Vault 和身份 Key | 全新 Chromium context，没有 catalog、Vault 或 active Key | 初始化事务完成，页面进入 Key 管理且刷新后身份仍存在 | Local 物理后端是 localStorage；Coordinator 最终一次性提交桶、Hold 快照和运行态；active publicKeyHex 可从浏览器业务结果读回 | 存储 bridge 未就绪；初始化提交失败后的回滚或恢复；刷新恢复失败 | local-browser | local-integration | J-LOCAL-INIT-MENU、J-LOCAL-CONTACT-MESSAGE、L-LEGACY-INITIAL-SETUP、L-LEGACY-LOCAL-INIT | 2026-09-12：pnpm test:e2e:integration 通过 9/9；初始化锁定/解锁 Journey 独立通过；pnpm test:e2e:initial-setup-smoke 通过 1/1。失败恢复仍未执行，S3 cold-start 由 J-REAL-S3-INIT 单独记录 | 部分覆盖 |
| KM-INIT-002 | 初始化与存储 | 在真实 S3 物理桶中建立第一个 Keymaster 逻辑桶、Vault 和身份 Key | s3.json 指定的真实物理桶已取得本轮 lease 并完成开场清理，浏览器是全新 Chromium context | S3 连接探测和初始化事务完成，远端对象已产生，页面刷新后仍能恢复同一逻辑桶和第一把 Key | S3 物理桶与 Keymaster 逻辑桶、对象 prefix 分层隔离；S3 provider 必须证明原生条件写入后才能初始化；Coordinator 一次性提交 Hold、目录 CAS 和运行态；S3 凭据与桶密码不进入 localStorage、运行状态或报告 | lease、开场清理或 prefix 清理失败；S3 CORS/原子条件写入不满足；初始化中途远端对象写入失败后的回滚或恢复；刷新时 S3 连接或快照读取失败；凭据错误或结果未知 | s3 | real-resource | J-REAL-S3-INIT | 2026-09-12：pnpm test:e2e:real-s3 的真实 S3 setup/teardown 通过，实际浏览器 Journey 在真实连接探测阶段被 provider CORS 阻断，未创建业务对象；修复目标桶 CORS 后可直接重跑 | 部分覆盖 |
| KM-VAULT-001 | 身份与 Key | 锁定、刷新后重新使用同一把 Key | Vault 已解锁且存在 active Key | 锁定撤销旧运行态，正确密码重新解锁同一身份 | session epoch/generation 失效旧权限；Key 私钥不暴露给 Window；持久化 Key 标签和 publicKeyHex 一致 | 错误密码；锁定过程中的并发操作；Key 读失败不能误判为空 Vault | local-browser | local-integration | J-LOCAL-INIT-MENU、L-LEGACY-INITIAL-SETUP | 2026-09-12：local-integration Journey 已验证刷新、锁定、正确密码重新解锁；错误密码和并发锁定仍未执行 | 部分覆盖 |
| KM-NAV-001 | 首页、菜单与设置 | 从正式菜单进入可用业务页面 | 应用装配完成且有 active Key | 导航入口、页面和返回路径与 manifest 一致 | business.registry 是菜单真值；route.registry 与 manifest 入口一致；插件装配失败不能假装 ready | 插件依赖缺失；未知 route；刷新后菜单恢复失败 | local-browser | local-integration | J-LOCAL-INIT-MENU | 2026-09-12：初始化完成后正式页面可用性通过；全正式菜单、设置入口和未知 route 仍待补 | 部分覆盖 |
| KM-CONTACT-001 | 联系人与消息 | 保存一个以公钥归属的联系人 | active Key 已解锁，联系人列表为空 | 联系人保存后可从列表读回且归属正确 active Key | canonical 身份是压缩 publicKeyHex；key-scoped 数据绑定 active Key；重复或非法身份不会静默覆盖 | 非法公钥；重复联系人；active Key 在编辑期间变化 | local-browser | local-integration | J-LOCAL-CONTACT-MESSAGE | 2026-09-12：联系人保存、当前 active Key 归属和会话入口通过；重复联系人及 active Key 切换仍未执行 | 部分覆盖 |
| KM-MESSAGE-001 | 联系人与消息 | 打开正确对端的会话并收发消息 | active Key 已解锁，消息服务已装配 | 非法输入在表单内失败，合法 publicKeyHex 打开对应会话 | 消息按 sender/recipient 身份域归属；消息页使用正式 parser/schema 和 resource store；页面打开不等于远端消息已发送 | 非法 publicKeyHex；Channel 不可用；发送超时或结果未知；刷新后历史恢复 | local-browser | local-integration | J-LOCAL-CONTACT-MESSAGE | 2026-09-12：非法公钥被表单阻断、合法公钥打开正确会话通过；真实 Channel 收发、刷新历史仍未执行 | 部分覆盖 |
| KM-ASSET-001 | 资产与转账 | 查看余额、选择资产并完成受控转账 | 已有 active Key 和对应网络地址 | 余额按网络/provider 聚合，转账状态和链上结果可对账 | mainnet/testnet 明确分离；本地提交状态与链上确认状态分开；输入和保护 outpoint 经过正式服务校验 | 余额不足；网络或 provider 不可用；广播结果未知；冲突/reorg 后恢复 | testnet | real-resource | J-REAL-TESTNET-ASSET、L-LEGACY-P2PKH | 已实现真实 testnet 充值、页面转账和归集 Journey；当前运行未执行：需要仓库外受保护 testnet 资源和实时网络确认；L-LEGACY-P2PKH 仅保留真实 provider 读取与交易路由 smoke | 部分覆盖 |
| KM-SATSUB-001 | SatSubscription | 在 testnet 上完成充值、消费和账本对账 | 配置通过路径权限、服务身份和 testnet 网络门禁 | WebSocket/WebRTC Direct 连接、请求幂等和双方账本一致 | 运行时确认服务身份和 testnet；request_id 在付款方/发布方域内唯一；结果未知先对账不盲目重试 | 配置错误；服务不可达；mainnet 身份；重复请求；支付结果未知 | satsubscription | real-resource | J-REAL-SATSUB-HEALTH | 2026-09-12：已加入真实资源 WebSocket/testnet 身份健康 Journey；充值、消费、重复 request_id、WebRTC Direct 和双方账本对账仍未执行 | 部分覆盖 |
| KM-MSFILE-001 | MSFile 与 P2P | 通过受控 P2P 读取文件、Range 和媒体数据 | 双方身份、Channel 和资源 lease 均有效 | 文件范围、取消、重连和媒体 Worker 结果可验证 | Host/lease 只有一个权威拥有者；Range parser 使用生产 schema；撤权先于 drain 和不可逆 I/O | 对端不可达；范围非法；传输超时；取消/锁定期间晚到消息；Worker 重启 | p2p | real-resource | L-LEGACY-MSFILE-EXECUTOR、L-LEGACY-MSFILE-RANGE、L-LEGACY-MSFILE-RUNTIME | 旧 MSFile executor、Range、媒体和 supplier runtime 测试已显式登记为本地证据；真实远端 supplier/P2P 资源 Journey 尚未执行 | 部分覆盖 |
| KM-APPS-001 | Apps、Connect 与公开协议 | 授权外部 App 连接并安全完成协议请求 | 外部 App 具有明确 origin、app identity 和 session 请求 | Popup handshake、权限、请求响应和取消均按正式协议完成 | 协议版本和 schema 严格校验；origin/app identity/session 绑定；MessagePort 关闭和撤权先于后续 I/O | origin 不匹配；版本错误；用户拒绝；popup 关闭；session 过期 | deployment | deployment-acceptance | J-DEPLOYMENT-APPS、L-LEGACY-LIFECYCLE-IO、L-LEGACY-LIFECYCLE-PRODUCTION、L-LEGACY-LIFECYCLE-RECOVERY | 部署 Journey 已绑定不可变 Build ID 响应头门禁；旧 lifecycle/Connect 测试已登记但目标部署的 Connect/Popup 完整业务链尚未执行 | 部分覆盖 |
| KM-SETTINGS-001 | 首页、菜单与设置 | 查看和修改语言、插件、系统及日志设置 | 应用装配完成，权限和配置状态可读取 | 设置通过正式 registry 生效并在刷新后保持合法状态 | 设置入口由 settings/system-settings registry 提供；日志和诊断默认脱敏；插件启停不破坏 capability 依赖 | 配置无效；插件依赖缺失；日志读取失败；刷新/重启恢复失败 | local-browser | local-integration | J-LOCAL-SETTINGS | local-integration 已验证正式设置入口、语言持久化和插件/日志/系统状态读取；错误配置与依赖缺失恢复仍未执行 | 部分覆盖 |
| KM-TECH-001 | 运行时与存储边界 | 确保真实浏览器中的存储、Worker 和运行时边界成立 | 真实 Chromium 打开生产 preview，未注入 Node 替身 | localStorage、Web Crypto、SharedWorker 和应用启动状态可观察 | Window → SharedWorker → localStorage bridge 是真实链路；Worker 重启后 epoch/generation 能隔离旧请求；URL 改变不等于业务 ready | Worker 不可用；端口晚到；bridge lease 失效；启动失败或恢复记录不可读 | local-browser | local-integration | G-STORAGE-BROWSER-BOUNDARY、L-LEGACY-HELLO、L-LEGACY-MSFILE-RANGE、L-LEGACY-LIFECYCLE-PRODUCTION | 2026-09-12：pnpm test:e2e:integration 通过 9/9，真实 Chromium storage-browser Gate 通过；只覆盖 localStorage/能力边界，Worker 重启与 bridge 失效恢复仍未执行 | 部分覆盖 |
| KM-TECH-002 | 运行时与存储边界 | 确保秘密不越过浏览器、日志和报告边界 | Node Resource 持有短生命周期秘密并准备附件，配置目录位于仓库外 | SecretString 默认脱敏，秘密形状在附件上传前阻断，错误权限在资源读取前拒绝 | 长期 seed/S3 Secret 只在 Node Resource；known secret、私钥 hex、WIF 均被扫描；阻断时不创建附件；配置目录和文件权限 fail-closed | 故意注入私钥形状；配置原文进入错误；序列化普通对象泄漏秘密；配置目录权限过宽 | none | local-integration | G-REDACTION-BOUNDARY、G-CONFIG-SAFETY | 2026-09-12：redaction Gate 和 config-safety Gate 通过；仅验证本地脱敏/配置边界，真实报告上传管线仍未执行 | 部分覆盖 |
| KM-LIFECYCLE-001 | 插件生命周期与恢复 | 在权限撤销、并发和故障期间不让旧请求继续影响业务 | 多个页面或 Worker 正在使用同一 owner/session | revoke/fence/block admission 先于 drain/I/O，旧 epoch 请求失败且可恢复 | identity、epoch、generation 在 publication/CAS 前复核；并发冲突不会覆盖新状态；结果未知的不可逆操作不自动重放 | 锁定中晚到端口；CAS 冲突；Worker 重启；离线恢复；不可逆 I/O 超时 | local-browser | local-integration | G-LIFECYCLE-BOUNDARY、L-LEGACY-MSFILE-EXECUTOR、L-LEGACY-LIFECYCLE-IO、L-LEGACY-LIFECYCLE-PRODUCTION、L-LEGACY-LIFECYCLE-RECOVERY | 2026-09-12：lifecycle-boundary Gate 两项通过；旧生产 executor/recovery 测试已登记；真实 Worker 重启、多页面恢复和目标部署 drill 仍未执行 | 部分覆盖 |
| KM-RESOURCE-001 | 真实资源安全 | 使用仓库外配置指定的 S3/testnet/SatSubscription 资源并可靠清理 | 仓库外配置、权限和预算均可验证 | setup 获取排他 lease，测试按 run_id/prefix 隔离，teardown/恢复账本闭合 | 路径、owner、mode、symlink fail closed；S3 按测试是否声明 prefix 选择前缀清理或全量业务对象清理，并保留当前 lease；testnet 网络身份和余额运行时确认 | 配置缺失/权限过宽；S3 lease 冲突；清理失败；余额不足；资金结果未知；服务身份不符 | s3 | real-resource | G-RESOURCE-SAFETY | 2026-09-12：真实配置桶的 S3 setup、G-RESOURCE-SAFETY prefix 清理和 teardown 均通过，执行后对象数为 0；testnet/SatSubscription 资源验收仍未执行 | 部分覆盖 |

## 资源与清理索引

| 编号 | 资源声明 | 清理规则 |
| --- | --- | --- |
| KM-INIT-001 | local-browser | 独立 BrowserContext 关闭后丢弃本地测试数据 |
| KM-INIT-002 | s3 | 前缀 Journey 使用 run_id/scenario_id 清理自己的 prefix；非前缀 setup/teardown 不传 prefix，清理指定桶全部业务对象并在完成后释放 lease |
| KM-VAULT-001 | local-browser | BrowserContext 关闭清理本地数据 |
| KM-NAV-001 | local-browser | 不产生远端状态 |
| KM-CONTACT-001 | local-browser | 独立 BrowserContext 关闭清理联系人 |
| KM-MESSAGE-001 | local-browser | 当前场景只验证入口，不产生远端 Channel 状态 |
| KM-ASSET-001 | testnet | 使用一次性钱包，归集剩余 testnet 资金并保留脱敏账本 |
| KM-SATSUB-001 | satsubscription | 归集客户端剩余资金；服务端正式账本不删除伪造恢复 |
| KM-MSFILE-001 | p2p | 只使用临时测试对象，结束关闭 lease 和远端连接 |
| KM-APPS-001 | deployment | 只清理本次 session，不保留外部 App 凭据 |
| KM-SETTINGS-001 | local-browser | 本地测试配置随独立 context 丢弃 |
| KM-TECH-001 | local-browser | 不保留浏览器 context 之外的状态 |
| KM-TECH-002 | none | 清零短期秘密，不产生远端状态 |
| KM-LIFECYCLE-001 | local-browser | 无外部资源时关闭 context；真实资源由专门账本收尾 |
| KM-RESOURCE-001 | s3 | 前缀测试只清理 prefix；非前缀测试清理指定桶全部业务对象；结束后释放 lease，失败由下一轮恢复 |

## 现有 E2E 迁移目录

> `L-` 编号表示仍由默认 E2E 命令执行的旧测试；它们已显式登记，但不冒充新的 integration Journey。

| 编号 | 文件 | 层级 | 需求 | 状态 | 说明 |
| --- | --- | --- | --- | --- | --- |
| L-LEGACY-HELLO | e2e/hello.spec.ts | local-integration | KM-TECH-001 | 保留 | 生产壳和浏览器原生能力 smoke；与 storage-browser Gate 的证据范围不同，待合并重复断言。 |
| L-LEGACY-INITIAL-SETUP | e2e/initial-setup.spec.ts | local-integration | KM-INIT-001、KM-VAULT-001 | 保留 | 旧初始化页面断言；迁移到 initializeLocalUser Flow 后保留导入与创建分支。 |
| L-LEGACY-LOCAL-INIT | e2e/local-initialization-smoke.spec.ts | local-integration | KM-INIT-001 | 保留 | 本地初始化 smoke 和浏览器错误证据；待与本地初始化 Journey 合并。 |
| L-LEGACY-MSFILE-EXECUTOR | e2e/msfile-executor-spike.spec.ts | local-integration | KM-MSFILE-001、KM-LIFECYCLE-001 | spike | 真实 Go/Noise/Window executor 技术边界；在真实远端 supplier Journey 建成前保留。 |
| L-LEGACY-MSFILE-RANGE | e2e/msfile-native-range.spec.ts | local-integration | KM-MSFILE-001、KM-TECH-001 | 保留 | 生产 Service Worker、媒体和 Range 边界；目前不是公网 P2P 验收。 |
| L-LEGACY-MSFILE-RUNTIME | e2e/msfile-production-runtime.spec.ts | local-integration | KM-MSFILE-001、KM-APPS-001、KM-LIFECYCLE-001 | 保留 | 本地 Go supplier 与真实浏览器 runtime 链路；外部服务身份和目标部署仍需独立验收。 |
| L-LEGACY-P2PKH | e2e/p2pkh-wallet.spec.ts | local-integration | KM-ASSET-001 | 保留 | 真实 provider 读取和交易路由 smoke；真实 testnet 充值、转账、确认和归集由 J-REAL-TESTNET-ASSET 负责。 |
| L-LEGACY-LIFECYCLE-IO | e2e/plugin-lifecycle-irreversible-io.spec.ts | deployment-acceptance | KM-APPS-001、KM-LIFECYCLE-001 | 保留 | 目标部署不可逆 I/O runner；没有配置目标 URL 时按显式 skip，不升级为通过。 |
| L-LEGACY-LIFECYCLE-PRODUCTION | e2e/plugin-lifecycle-production.spec.ts | local-integration | KM-APPS-001、KM-LIFECYCLE-001、KM-TECH-001 | 保留 | 真实生产浏览器生命周期、SharedWorker 和撤权边界；已部署外部 App 分支需配置后单独验收。 |
| L-LEGACY-LIFECYCLE-RECOVERY | e2e/plugin-lifecycle-recovery.spec.ts | deployment-acceptance | KM-LIFECYCLE-001、KM-APPS-001 | 保留 | 目标部署 Coordinator recovery drill；未配置目标 URL 时按显式 skip，不伪造恢复证据。 |
