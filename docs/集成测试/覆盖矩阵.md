# 覆盖矩阵（自动生成）

> 真值来源：`覆盖矩阵.yaml`。本页只展示需求和证据边界，不把未执行的真实资源或部署测试标成通过。

共 18 项需求：已覆盖 0，部分覆盖 18，未覆盖 0，阻断 0。

| 编号 | 业务域 | 用户目标 | 开始状态 | 成功结果 | 技术事实 | 失败/恢复 | 资源 | 层级 | 场景 | 证据 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| KM-INIT-001 | 初始化与存储 | 建立第一个 Local 桶、Vault 和身份 Key | 全新 Chromium context，没有 catalog、Vault 或 active Key | 初始化事务完成，页面进入首页且刷新后身份仍存在 | Local 物理后端是 localStorage；Coordinator 最终一次性提交桶、Hold 快照和运行态；active publicKeyHex 可从浏览器业务结果读回 | 存储 bridge 未就绪；初始化提交失败后的回滚或恢复；刷新恢复失败 | local-browser | local-integration | J-LOCAL-INIT-MENU、J-LOCAL-INIT-IMPORTED、J-LOCAL-CONTACT-MESSAGE、G-COORDINATOR-DEV-HTTP | 本地生成 Key、导入 Hex Key、跨业务初始化和非安全 HTTP HMAC 提交均已有独立 integration 场景；失败回滚和 S3 cold-start 仍由对应层级记录 | 部分覆盖 |
| KM-INIT-002 | 初始化与存储 | 在真实 S3 物理桶中建立第一个 Keymaster 逻辑桶、Vault 和身份 Key | s3.json 指定的真实物理桶已取得本轮 lease 并完成开场清理，浏览器是全新 Chromium context | S3 连接探测和初始化事务完成，远端对象已产生，页面刷新后仍能恢复同一逻辑桶和第一把 Key | S3 物理桶与 Keymaster 逻辑桶、对象 prefix 分层隔离；S3 provider 优先使用原生条件写；服务忽略 If-Match 时降级为 best-effort（HEAD 读 ETag 后写入，非原子）；探测结果写入 keymaster.device.v1 的 capabilities 缓存，之后连接/列 Keys 不再自动探测，仅支持手工重新探测；Coordinator 一次性提交 Hold、目录 CAS 和运行态；S3 凭据与桶密码不进入 localStorage、运行状态或报告 | lease、开场清理或 prefix 清理失败；S3 CORS/原子条件写入不满足；初始化中途远端对象写入失败后的回滚或恢复；刷新时 S3 连接或快照读取失败；凭据错误或结果未知 | s3 | s3 | J-REAL-S3-INIT | 2026-09-12：pnpm test:e2e:s3 的真实 S3 setup/teardown 通过，实际浏览器 Journey 在真实连接探测阶段被 provider CORS 阻断，未创建业务对象；修复目标桶 CORS 后可直接重跑 | 部分覆盖 |
| KM-VAULT-001 | 身份与 Key | 锁定、刷新后重新使用同一把 Key | Vault 已解锁且存在 active Key | 锁定撤销旧运行态，正确密码重新解锁同一身份并回到已解锁首页 | session epoch/generation 失效旧权限；Key 私钥不暴露给 Window；持久化 Key 标签和 publicKeyHex 一致 | 错误密码；锁定过程中的并发操作；Key 读失败不能误判为空 Vault | local-browser | local-integration | J-LOCAL-INIT-MENU、J-LOCAL-INIT-IMPORTED | 本地生成 Key Journey 已验证刷新、锁定、正确密码重新解锁；导入 Key Journey 验证导入结果；错误密码和并发锁定仍未执行 | 部分覆盖 |
| KM-NAV-001 | 首页、菜单与设置 | 从正式菜单进入可用业务页面 | 应用装配完成且有 active Key | 导航入口、页面和返回路径与 manifest 一致 | business.registry 是菜单真值；route.registry 与 manifest 入口一致；插件装配失败不能假装 ready | 插件依赖缺失；未知 route；刷新后菜单恢复失败 | local-browser | local-integration | J-LOCAL-INIT-MENU | 2026-09-12：初始化完成后正式页面可用性通过；全正式菜单、设置入口和未知 route 仍待补 | 部分覆盖 |
| KM-CONTACT-001 | 联系人与消息 | 保存一个以公钥归属的联系人 | active Key 已解锁，联系人列表为空 | 联系人保存后可从列表读回且归属正确 active Key | canonical 身份是压缩 publicKeyHex；key-scoped 数据绑定 active Key；重复或非法身份不会静默覆盖 | 非法公钥；重复联系人；active Key 在编辑期间变化 | local-browser | local-integration | J-LOCAL-CONTACT-MESSAGE、J-REAL-TESTNET-CONTACT-TRANSFER | 2026-09-12：联系人保存、当前 active Key 归属和会话入口通过；真实 testnet 通讯录收款方与页面转账 Journey 已登记，待受保护资源执行；重复联系人及 active Key 切换仍未执行 | 部分覆盖 |
| KM-MESSAGE-001 | 联系人与消息 | 打开正确对端的会话并收发消息 | active Key 已解锁，消息服务已装配 | 非法输入在表单内失败，合法 publicKeyHex 打开对应会话 | 消息按 sender/recipient 身份域归属；消息页使用正式 parser/schema 和 resource store；页面打开不等于远端消息已发送 | 非法 publicKeyHex；Channel 不可用；发送超时或结果未知；刷新后历史恢复 | local-browser | local-integration | J-LOCAL-CONTACT-MESSAGE、J-REAL-SATSUB-MESSAGE | 2026-09-18：新增 J-REAL-SATSUB-MESSAGE，从仓库外 SatSubscription 构建正式服务并使用一次性 PostgreSQL，验证真实 Channel 私信收发、刷新后本地历史、独立浏览器隔离和账本 0 扣费；2026-09-19：消息历史切换为 KeymasterFormats messages 文件格式（sent/received raw 内容寻址 + timeindex 只增索引，raw 缺失显示缺失），同一 Journey 校验桶内文件、seed_hash 与缺失展示；公网供应商和收费路径仍未执行 | 部分覆盖 |
| KM-ASSET-001 | 资产与转账 | 查看余额、选择资产并完成受控转账 | 已有 active Key 和对应网络地址 | 余额按网络/provider 聚合，转账状态和链上结果可对账 | mainnet/testnet 明确分离；本地提交状态与链上确认状态分开；输入和保护 outpoint 经过正式服务校验 | 余额不足；网络或 provider 不可用；广播结果未知；冲突/reorg 后恢复 | testnet | p2pkh | J-REAL-TESTNET-ROUNDTRIP、J-REAL-TESTNET-CONTACT-TRANSFER、J-LOCAL-P2PKH-NAVIGATION、J-REAL-BITFS-PURCHASE | 2026-09-21：J-REAL-TESTNET-ROUNDTRIP 已真实跑通 50 sat 全部回款；J-REAL-TESTNET-CONTACT-TRANSFER 登记了 200 sat、五笔页面广播和 funding/change 输入衔接，待受保护资源执行；余额不足、provider 不可用、冲突/reorg 等失败路径仍未覆盖。 | 部分覆盖 |
| KM-BALANCE-001 | 全局余额广播 | 在不同页面和 tab 中看到及时、正确且可解释的网络余额 | active Key 已解锁，testnet 设置和 UTXO 快照可由正式页面刷新 | 金额旁余额参考与钱包/资产总览一致；tab 刷新和 testnet 开关传播不会覆盖用户正在填写的金额 | 余额由全局 balance broadcaster 按网络发布；UTXO 刷新后余额参考及时失效并重读；未知余额显示未知而不是伪造 0 | 快照未就绪；跨 tab 刷新延迟；testnet 被关闭；广播后余额仍停留旧值 | testnet | p2pkh | J-REAL-TESTNET-CONTACT-TRANSFER | 已登记 B1-B5 页面与真实链上验证步骤，待受保护 testnet 资源执行。 | 部分覆盖 |
| KM-BROADCAST-001 | 中心广播与 UTXO 序号门禁 | 在 UTXO 快照变化和广播结果不确定时不重复花费或错误重放 | key01 已收到真实 testnet 资金，页面通过正式中心广播服务准备交易 | 连打时按新快照序号重建并继续；明确未派发不消费序号；sendAll 重新确认次数有限；链上输入输出与页面结果一致 | 广播入口唯一且提交前校验快照序号；snapshot-consumed/stale 只在确定未派发时等待新序号；isolated 结果不能自动重发或归集；local-confirmed txid 必须能从原始交易对账 | 余额不足；snapshot stale/consumed；sendAll requires-reconfirm；isolated/结果未知；WoC 传播与 raw hex 索引延迟 | testnet | p2pkh | J-REAL-TESTNET-CONTACT-TRANSFER、J-REAL-BITFS-PURCHASE | 已登记 T1-T5 页面广播、T2 连打、T4.5 未派发和 T5 sendAll 链上对账步骤；BitFS 购买使用同一真实广播与专款保护边界。 | 部分覆盖 |
| KM-SATSUB-001 | SatSubscription | 在 testnet 上完成充值、消费和账本对账 | 配置通过路径权限、服务身份和 testnet 网络门禁 | 真实页面保存供应商配置，并按页面结果区分正确身份 online 与错误身份 disconnected/degraded；充值、消费和账本对账仍需独立业务 Journey | 真实页面把 websocket/webrtc-direct multiaddr 映射为 supplier.multiaddrs；页面连接必须校验 supplierPublicKeyHex 身份 pin；request_id 在付款方/发布方域内唯一；结果未知先对账不盲目重试 | 配置错误；服务不可达；mainnet 身份；重复请求；支付结果未知 | satsubscription | satsubscription | J-REAL-SATSUB-HEALTH、J-REAL-SATSUB-PAGE、J-REAL-SATSUB-MESSAGE、J-REAL-SATSUB-SERVER-SETTINGS、J-REAL-SATSUB-DEFAULT-SETTINGS、J-REAL-BITFS-PURCHASE | 2026-09-14：新增真实 Chromium /settings/system-status 广播网关 Journey，使用仓库外 satsubscription.json 的 multiaddr 和 publickeyhex，先比较错误 pin 的页面失败，再验证正确 pin 的页面 online；2026-09-18：J-REAL-SATSUB-MESSAGE 从仓库外源码启动正式服务与一次性 PostgreSQL，验证真实 /ssp/1.0.0 订阅、Publish 广播和白名单 0 扣费；2026-09-20：J-REAL-SATSUB-SERVER-SETTINGS 单免费用户验证本地供应商红绿灯 online、刷新 SPI 余额、刷新远端订阅，一次性库预置 3 条正式正扣费账单后以每页 2 条完成真实 SSP 查询第 1 页→第 2 页→第 1 页翻页（同一会话复用 fromMs/toMs/limit）；充值、消费、重复 request_id、WebRTC Direct 和双方账本对账仍未执行；2026-09-20：J-REAL-SATSUB-DEFAULT-SETTINGS 直连内置 bsv8 缺省网关验证红绿灯 online 与 SPI/订阅/空账单只读查询 | 部分覆盖 |
| KM-MSFILE-001 | MSFile 与 P2P | 通过受控 P2P 读取文件、Range 和媒体数据 | 双方身份、Channel 和资源 lease 均有效 | 文件范围、取消、重连和媒体 Worker 结果可验证 | Host/lease 只有一个权威拥有者；Range parser 使用生产 schema；撤权先于 drain 和不可逆 I/O；local 来源不编码 MSFile wire、不伪造供应商公钥；卖方只响应命中完整 Seed 索引且 locator 兼容的已验证 Hash 请求 | 对端不可达；范围非法；传输超时；取消/锁定期间晚到消息；Worker 重启；本地缺块/损坏块；卖方协议端口未就绪 | p2p | msfile | G-MSFILE-EXECUTOR、G-MSFILE-NATIVE-RANGE、G-MSFILE-PRODUCTION-RUNTIME、J-REAL-MSFILE-NAS、J-REAL-MSFILE-OFFICIAL、J-REAL-BITFS-PURCHASE | 2026-09-19：MSFile 内置 BSV8 官方供应商（系统缺省、不可删除）；J-REAL-MSFILE-NAS 用真实 Chromium 页面在 /settings/local-files 保存金额上限并 pin cmd/msfile-nas 的 WebRTC Direct 地址，校验文本预览、二进制下载 SHA-256 对账和供应商 Read 计数；J-REAL-MSFILE-OFFICIAL 对官方 msfiles 服务的 wav/mp3/mp4 做原生 Range 播放打开、Hello.md 做文本预览打开，并记录 Range/206 证据；2026-09-22：local-bitfs 来源、卖方设置与 Seed 索引、已验证 Hash 请求匹配与 BitFS stream 通道已有单元测试证据（Worker 集成测试覆盖页面上传后命中索引、重复/未命中静默、会话建立与关闭），真实公网供应商写入/发布 Journey 与 WebSocket/WSS、WebRTC Direct 浏览器互操作仍未执行 | 部分覆盖 |
| KM-APPS-001 | Apps、Connect 与公开协议 | 授权外部 App 连接并安全完成协议请求 | 外部 App 具有明确 origin、app identity 和 session 请求 | Popup handshake、权限、请求响应和取消均按正式协议完成 | 协议版本和 schema 严格校验；origin/app identity/session 绑定；MessagePort 关闭和撤权先于后续 I/O | origin 不匹配；版本错误；用户拒绝；popup 关闭；session 过期 | deployment | deployment-acceptance | J-DEPLOYMENT-APPS、J-DEPLOYMENT-APPVIEW-CONNECT、G-DEPLOYMENT-IRREVERSIBLE-IO、G-DEPLOYMENT-COORDINATOR-RECOVERY、G-MSFILE-PRODUCTION-RUNTIME | 部署 Build ID 和外部 AppView Journey、不可逆 I/O Gate、Coordinator recovery Gate 已归一化；目标部署 Connect/Popup 需受保护环境执行 | 部分覆盖 |
| KM-SETTINGS-001 | 首页、菜单与设置 | 查看和修改自动锁屏、智能调度、BSV 链、本地文件、插件及广播网关 | 应用装配完成，权限和配置状态可读取 | 各独立设置通过正式 registry 生效并在刷新后保持合法状态 | 自动锁屏与智能调度分别由 Vault 和 Background 插件注册到设置域；BSV 链页面在设置域内组合 P2PKH 与 WOC 配置；本地文件页面由 MSFile 插件注册到设置域；插件启停不破坏 capability 依赖 | 配置无效；插件依赖缺失；刷新/重启恢复失败 | local-browser | local-integration | J-LOCAL-SATSUB-DEFAULT、J-LOCAL-SETTINGS、J-REAL-SATSUB-SERVER-SETTINGS | local-integration 已验证自动锁屏、智能调度、BSV 链、本地文件、插件和广播网关独立入口；2026-09-20：J-REAL-SATSUB-SERVER-SETTINGS 验证本地 SS 供应商红绿灯与三功能真实翻页（不测内置 bsv8）；错误配置与依赖缺失恢复仍未执行 | 部分覆盖 |
| KM-TECH-001 | 运行时与存储边界 | 确保真实浏览器中的存储、Worker 和运行时边界成立 | 真实 Chromium 打开生产 preview，未注入 Node 替身 | IndexedDB、localStorage 引导记录、Web Crypto、SharedWorker 和应用启动状态可观察 | Window → SharedWorker → IndexedDB bridge 是真实链路，localStorage 只保留设备引导记录；Worker 重启后 epoch/generation 能隔离旧请求；URL 改变不等于业务 ready | Worker 不可用；端口晚到；bridge lease 失效；启动失败或恢复记录不可读 | local-browser | local-integration | G-STORAGE-BROWSER-BOUNDARY、G-COORDINATOR-DEV-HTTP、G-COORDINATOR-RUNTIME-LIFECYCLE、G-PLUGIN-LIFECYCLE-PRODUCTION、G-MSFILE-NATIVE-RANGE、J-LOCAL-MULTI-TAB-RECOVERY | storage、非安全 HTTP、Coordinator 生命周期、插件生命周期、多标签刷新和 MSFile SW Gate 已进入统一执行档；Worker 重启与 bridge 失效的部分恢复仍需持续补齐 | 部分覆盖 |
| KM-TECH-002 | 运行时与存储边界 | 确保秘密不越过浏览器、日志和报告边界 | Node Resource 持有短生命周期秘密并准备附件，配置目录位于仓库外 | SecretString 默认脱敏，秘密形状在附件上传前阻断，错误权限在资源读取前拒绝 | 长期 seed/S3 Secret 只在 Node Resource；known secret、私钥 hex、WIF 均被扫描；阻断时不创建附件；配置目录和文件权限 fail-closed | 故意注入私钥形状；配置原文进入错误；序列化普通对象泄漏秘密；配置目录权限过宽 | none | local-integration | G-REDACTION-BOUNDARY、G-CONFIG-SAFETY | 2026-09-12：redaction Gate 和 config-safety Gate 通过；仅验证本地脱敏/配置边界，真实报告上传管线仍未执行 | 部分覆盖 |
| KM-LIFECYCLE-001 | 插件生命周期与恢复 | 在权限撤销、并发和故障期间不让旧请求继续影响业务 | 多个页面或 Worker 正在使用同一 owner/session | revoke/fence/block admission 先于 drain/I/O，旧 epoch 请求失败且可恢复 | identity、epoch、generation 在 publication/CAS 前复核；并发冲突不会覆盖新状态；结果未知的不可逆操作不自动重放 | 锁定中晚到端口；CAS 冲突；Worker 重启；离线恢复；不可逆 I/O 超时 | local-browser | local-integration | G-LIFECYCLE-BOUNDARY、G-COORDINATOR-DEV-HTTP、G-COORDINATOR-RUNTIME-LIFECYCLE、G-PLUGIN-LIFECYCLE-PRODUCTION、G-MSFILE-EXECUTOR、G-MSFILE-NATIVE-RANGE、G-MSFILE-PRODUCTION-RUNTIME、G-DEPLOYMENT-IRREVERSIBLE-IO、G-DEPLOYMENT-COORDINATOR-RECOVERY、J-LOCAL-SATSUB-DEFAULT、J-LOCAL-MULTI-TAB-RECOVERY | lifecycle、Coordinator、MSFile 和目标部署恢复证据均已登记到统一 Gate；真实 Worker 重启、多页面生产恢复和目标部署 drill 仍需受保护环境执行 | 部分覆盖 |
| KM-RESOURCE-001 | 真实资源安全 | 使用仓库外配置指定的 S3/testnet/SatSubscription 资源并可靠清理 | 仓库外配置、权限和预算均可验证 | setup 获取排他 lease，测试按 run_id/prefix 隔离，teardown/恢复账本闭合 | 路径、owner、mode、symlink fail closed；S3 按测试是否声明 prefix 选择前缀清理或全量业务对象清理，并保留当前 lease；testnet 网络身份和余额运行时确认 | 配置缺失/权限过宽；S3 lease 冲突；清理失败；余额不足；资金结果未知；服务身份不符 | s3 | s3 | G-RESOURCE-SAFETY | 2026-09-12：真实配置桶的 S3 setup、G-RESOURCE-SAFETY prefix 清理和 teardown 均通过，执行后对象数为 0；testnet/SatSubscription 资源验收仍未执行 | 部分覆盖 |
| KM-STORAGE-001 | 存储与身份切换 | 在多个桶和每个桶内的多把 Key 之间交叉切换，并确认当前身份正确 | s3.json 指定的真实物理桶已取得本轮 lease 并完成开场清理；浏览器从全新 Local 桶开始 | 每次切换后首页“我的信息”公钥与 session.activeKey 都等于目标 Key；本机目录登记两个桶；真实 S3 前缀下 keys/ 只有本场景两把 KeyHold | 跨桶切换先在临时 Provider 上验证目标 Key 密码，失败不改变当前运行态；Local 桶 Keys 无需桶密码即可读取，S3 桶必须先输入桶密码读取 Keys；/storage/buckets 的新建 Key 与初始化首 Key 走同一条 KeyHold 单文件真值路径；切换成功后立即清除内存中暂存的桶密码；Local 桶 Key 在非当前状态也可列出并删除（KeyHold + owner 数据），不改变当前运行态 | Key 密码错误或取消；S3 桶密码错误导致无法读取 Keys；切换过程中页面刷新或旧运行态被迟到结果覆盖 | s3 | s3 | J-REAL-S3-BUCKET-KEY-SWITCH | 2026-09-18：pnpm test:e2e:s3 的 J-REAL-S3-BUCKET-KEY-SWITCH 真实通过（Local+S3 双桶双 Key 交叉切换、桶管理页列出当前 S3 与非当前 Local 桶 Key、删除非当前 Local Key 的 KeyHold 与 owner 数据；真实 S3 前缀校验）；失败路径（错误密码/取消）仍未覆盖 | 部分覆盖 |

## 资源与清理索引

| 编号 | 资源声明 | 清理规则 |
| --- | --- | --- |
| KM-INIT-001 | local-browser | 独立 BrowserContext 关闭后丢弃本地测试数据 |
| KM-INIT-002 | s3 | 前缀 Journey 使用 run_id/scenario_id 清理自己的 prefix；非前缀 setup/teardown 不传 prefix，清理指定桶全部业务对象并在完成后释放 lease |
| KM-VAULT-001 | local-browser | BrowserContext 关闭清理本地数据 |
| KM-NAV-001 | local-browser | 不产生远端状态 |
| KM-CONTACT-001 | local-browser | 独立 BrowserContext 关闭清理联系人 |
| KM-MESSAGE-001 | local-browser | 页面入口场景关闭 context 清理；真实供应商场景关闭独立浏览器、临时 PostgreSQL 和 SatSubscription 进程 |
| KM-ASSET-001 | testnet | 固定 key01 钱包；失败时 Node 按链上事实归集回 seed，遗留余额用 pnpm collect:testnet:key01 手工归集；不保留跨轮账本 |
| KM-BALANCE-001 | testnet | 固定 key01 钱包；页面广播后按链上事实完成全部余额回收，未知结果不自动归集 |
| KM-BROADCAST-001 | testnet | 任意页面广播进入后保留现场并按链上事实处理；正常完成由 T5 全部发送清零，广播前失败才允许 Node 归集 |
| KM-SATSUB-001 | satsubscription | 页面 Journey 只建立并关闭本地连接；若执行充值则归集客户端剩余资金，服务端正式账本不删除伪造恢复 |
| KM-MSFILE-001 | p2p | 只使用临时测试对象，结束关闭 lease 和远端连接 |
| KM-APPS-001 | deployment | 只清理本次 session，不保留外部 App 凭据 |
| KM-SETTINGS-001 | local-browser | 本地测试配置随独立 context 丢弃 |
| KM-TECH-001 | local-browser | 不保留浏览器 context 之外的状态 |
| KM-TECH-002 | none | 清零短期秘密，不产生远端状态 |
| KM-LIFECYCLE-001 | local-browser | 无外部资源时关闭 context；真实资源由专门账本收尾 |
| KM-RESOURCE-001 | s3 | 前缀测试只清理 prefix；非前缀测试清理指定桶全部业务对象；结束后释放 lease，失败由下一轮恢复 |
| KM-STORAGE-001 | s3 | 前缀 Journey 只清理自己的 run_id/scenario_id prefix；非前缀 teardown 清理全部业务对象并在确认后释放 lease |

## 执行档边界

| 执行档 | 目录 | 中文含义 |
| --- | --- | --- |
| local-core | `journeys/local`、`gates/local` | 普通本地浏览器 Journey 与本地 Gate |
| dev-http | `gates/dev-http` | 非安全 HTTP 开发服务器回归 |
| lifecycle | `gates/lifecycle` | 本地/registry Coordinator 生命周期验收 |
| msfile | `journeys/msfile`、`gates/msfile` | 需要临时 Go supplier 的 MSFile 页面 Journey 与技术 Gate |
| bitfs | `journeys/bitfs` | 双浏览器 BitFS 买卖与 testnet 购买验收 |
| p2pkh | `journeys/p2pkh` | 链上资产与转账（真实 testnet 资金） |
| satsubscription | `journeys/satsubscription` | SatSubscription 真实服务、页面与健康 Journey |
| s3 | `journeys/s3`、`gates/s3`、`resources/s3-resource-*` | 真实 S3 桶存储 Journey 与资源安全 Gate |
| resources | `resources/resource-setup`、`resource-teardown`、`resource-availability` | 受保护外部资源的准备、可用性与收尾 |
| deployment | `journeys/deployment`、`gates/deployment` | 目标部署和不可逆 I/O 验收 |