# 联系人与消息

联系人以 active Key 下的 `publicKeyHex` 作为唯一身份。消息历史同样按 owner 和对端归属，
刷新后不能串到另一把 Key。联系人创建、错误输入、会话入口和真实 Channel 收发需要分别记录
本地浏览器证据与外部服务证据。

当前 Journey 覆盖联系人保存和合法/非法会话入口；没有远端 Channel 连接时，发送/接收链路
保持未执行或失败，不把本地表单通过写成网络通信通过。
