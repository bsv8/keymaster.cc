packages:
  - "apps/*"
  - "packages/*"

linkWorkspacePackages: true

allowBuilds:
  esbuild: true

overrides:
  bitcoin-libp2p: 0.3.0

minimumReleaseAgeExclude:
  - bitcoin-libp2p@0.3.0
  # 当前各项目均固定 bsv8-channel-protocol 版本；按包名放行以兼容 CI 的锁文件校验。
  - bsv8-channel-protocol
